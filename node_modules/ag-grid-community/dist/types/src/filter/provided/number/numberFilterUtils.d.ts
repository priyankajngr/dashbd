import type { GridOptionsService } from '../../../gridOptionsService';
import type { Column } from '../../../interfaces/iColumn';
import type { Tuple } from '../iSimpleFilter';
import type { OptionsFactory } from '../optionsFactory';
import type { INumberFilterParams, NumberFilterModel } from './iNumberFilter';
export declare function getAllowedCharPattern(filterParams?: INumberFilterParams): string | null;
/** A pattern usually admits characters, and a formatter writes text, that a number input's own grammar rejects. */
export declare function usesTextInput(filterParams?: INumberFilterParams): boolean;
/** The one reading of a typed value: `numberParser` owns it wherever it is configured. */
export declare function stringToFloat(numberParser: INumberFilterParams['numberParser'], value: string | number | null | undefined, gos: GridOptionsService, column: Column): number | null;
export declare function processNumberFilterValue(value?: number | null): number | null;
export declare function mapValuesFromNumberFilterModel(filterModel: NumberFilterModel | null, optionsFactory: OptionsFactory): Tuple<number>;
