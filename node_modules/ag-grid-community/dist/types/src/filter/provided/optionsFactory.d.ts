import type { LogService } from '../../validation/logService';
import type { IFilterOptionDef, ISimpleFilterParams } from './iSimpleFilter';
export declare class OptionsFactory {
    private customFilterOptions;
    /** As configured, so a refresh compares what it was given rather than what it kept. */
    private configuredOptions;
    /** What the dropdown offers: the configured list minus its malformed entries, or the built-ins if none survive. */
    filterOptions: (IFilterOptionDef | string)[];
    private offeredOptions;
    defaultOption?: string;
    init(log: LogService, params: ISimpleFilterParams, defaultOptions: string[]): void;
    refresh(log: LogService, params: ISimpleFilterParams, defaultOptions: string[]): void;
    /** Rebuilt wholesale, so a `predicate` the previous list carried cannot survive into this one. */
    private buildOptions;
    private collectUsableOptions;
    private getDefaultItem;
    hasOption(key?: string | null): boolean;
    getCustomOption(name?: string | null): IFilterOptionDef | undefined;
}
