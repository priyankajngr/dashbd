import type { LocaleTextFunc } from 'ag-stack';
import type { FilterWrapperParams } from '../../interfaces/iFilter';
import type { LogService } from '../../validation/logService';
import type { FilterLocaleTextKey } from '../filterLocaleText';
import type { IProvidedFilterParams } from './iProvidedFilter';
import type { FilterOptionKey, FilterPlaceholderFunction } from './iSimpleFilter';
import type { OptionsFactory } from './optionsFactory';
export declare function getDebounceMs(log: LogService, params: IProvidedFilterParams, debounceDefault: number): number;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _isUseApplyButton(params: FilterWrapperParams): boolean;
/** A Custom Filter Option has no built-in locale entry, so its own `displayKey`/`displayName` are the lookup. */
export declare function translateFilterOption(bean: {
    getLocaleTextFunc(): LocaleTextFunc;
}, optionsFactory: OptionsFactory, filterOptionKey: FilterOptionKey): string;
export declare function getPlaceholderText(bean: {
    getLocaleTextFunc(): LocaleTextFunc;
}, filterPlaceholder: string | FilterPlaceholderFunction | undefined, defaultPlaceholder: FilterLocaleTextKey, filterOptionKey: FilterOptionKey, optionsFactory: OptionsFactory): string;
