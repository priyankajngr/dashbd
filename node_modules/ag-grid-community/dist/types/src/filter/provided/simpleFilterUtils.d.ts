import type { GridOptionsService } from '../../gridOptionsService';
import type { AdvancedFilterOnlyOptionKey } from '../../interfaces/advancedFilterModel';
import type { Column } from '../../interfaces/iColumn';
import type { FilterCallbackSource, FilterInputCallbackParams } from '../../interfaces/iFilter';
import type { LogService } from '../../validation/logService';
import type { FilterLocaleTextKey } from '../filterLocaleText';
import type { FilterOptionKey, IFilterOptionDef, JoinOperator, Tuple } from './iSimpleFilter';
import type { OptionsFactory } from './optionsFactory';
/**
 * Built per call, not per binding: `context` is a grid option, so a captured one would go stale.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function filterCallbackParams(gos: GridOptionsService, column: Column, source: FilterCallbackSource): FilterInputCallbackParams;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _bindFilterCallback<A, R>(callback: ((value: A, params: FilterInputCallbackParams) => R) | undefined, gos: GridOptionsService, column: Column | null | undefined, source: FilterCallbackSource): ((value: A) => R) | undefined;
/** `NaN` is below it too: every comparison against it is false, so left through it would cap nothing. */
export declare function isBelowConditionFloor(count: number): boolean;
/**
 * Absent where nothing was configured, so a model set through the API keeps every condition it was given;
 * otherwise whole and at least one, as the display counts conditions.
 */
export declare function getConditionLimit(maxNumConditions: number | undefined): number | null;
export declare function removeItems<T>(items: T[], startPosition: number, deleteCount?: number): T[];
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _isBlank<V>(cellValue: V): boolean;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _hasValue<V>(cellValue: V): boolean;
export declare function getDefaultJoinOperator(defaultJoinOperator?: JoinOperator): JoinOperator;
export declare function evaluateCustomFilter<V>(customFilterOption: IFilterOptionDef | undefined, values: Tuple<V>, cellValue: V | null | undefined, gos: GridOptionsService, column: Column): boolean | undefined;
export declare function validateAndUpdateConditions<M>(log: LogService, conditions: M[], maxNumConditions: number): number;
/**
 * What a column filter withholds from a `filterOptions` list, having no operator for any of it.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare const _ADVANCED_FILTER_ONLY_OPTIONS: Record<AdvancedFilterOnlyOptionKey, true>;
/**
 * One definition of what a `filterOptions` list offers, so the column filter and the Advanced Filter cannot disagree.
 * `excludedKeys` withholds the ones the caller has no operator for; a definition under such a key is its own
 * statement of what it means, so only a bare key is dropped.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function _classifyFilterOptions(configuredOptions: (IFilterOptionDef | string)[], warnMissing: (keys: string[]) => void, excludedKeys: Record<string, true> | null): {
    offered: Map<string, IFilterOptionDef | string>;
    customOptions: Map<string, IFilterOptionDef>;
};
/**
 * The name an option is shown and written under: localised text, then `displayName`, then its key.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function _getCustomOptionDisplayName(option: IFilterOptionDef, translate: (key: string, defaultValue: string) => string): string;
/**
 * How many values an option takes; the declared `0 | 1 | 2` is no check on a JS caller, hence the clamp.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function _getCustomOptionNumberOfInputs(option: IFilterOptionDef): number;
export declare function getNumberOfInputs(type: FilterOptionKey | null | undefined, optionsFactory: OptionsFactory): number;
/**
 * `from` must be below `to`, or equal where the range is inclusive: an inclusive range of one value is an
 * exact match, so only a strict one has nothing left to match.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function _isRangeOutOfOrder<V extends number | bigint | Date>(fromValue: V | null, toValue: V | null, inclusive?: boolean): boolean;
/**
 * The column filter's message for a range whose bounds are out of order: it goes on the end being edited
 * and names the other end, in the words of whichever kind of value it is.
 */
export declare function getValidityMessageKey<V extends number | bigint | Date>(fromValue: V | null, toValue: V | null, isFrom: boolean, inclusive?: boolean): FilterLocaleTextKey | null;
