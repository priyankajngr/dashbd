import type { NamedBean } from '../context/bean';
import { BeanStub } from '../context/beanStub';
export declare class TooltipTouchCoordinator extends BeanStub implements NamedBean {
    beanName: "tooltipTouchSvc";
    private readonly targets;
    private selectedTarget;
    private lastTouchEventTime;
    private lastTouchTarget;
    postConstruct(): void;
    registerSource(element: HTMLElement, canShow: () => boolean, show: (touchStart: Touch) => void): () => void;
    /**
     * Whether a mouse event on the given element is the compatibility event synthesised for a recent
     * touch. Scoped to the touched element so a real mouse hover elsewhere on a hybrid device still shows.
     */
    isCompatibilityMouseEvent(sourceElement: HTMLElement): boolean;
    private recordTouchAt;
    private startTracking;
    private resolveLongTapTarget;
    private getCandidates;
    private getEventPath;
    private unregisterSource;
    destroy(): void;
}
