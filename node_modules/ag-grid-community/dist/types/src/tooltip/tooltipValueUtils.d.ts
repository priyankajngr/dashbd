import type { AgColumn } from '../entities/agColumn';
import type { AbstractColDef, ColDef } from '../entities/colDef';
import type { TooltipCallbackParams } from './tooltipComponent';
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export type LegacyTooltipFieldResolution = {
    resolved: false;
} | {
    resolved: true;
    value: any;
};
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _resolveCellTooltipValue<TData, TValue, TContext>(colDef: ColDef<TData, TValue>, params: TooltipCallbackParams<TData, TValue, TContext>, getLegacyFieldValue: () => LegacyTooltipFieldResolution): any;
/**
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 * Group rows keep the inverse legacy precedence: `tooltipValueGetter` before `tooltipField`.
 */
export declare function _resolveGroupTooltipValue<TData, TValue, TContext>(colDef: ColDef<TData, TValue>, params: TooltipCallbackParams<TData, TValue, TContext>, getLegacyFieldValue: () => LegacyTooltipFieldResolution): any;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _resolveHeaderTooltipValue<TData, TValue, TContext>(colDef: AbstractColDef<TData, TValue> | null | undefined, params: TooltipCallbackParams<TData, TValue, TContext>, componentValue?: any): any;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _isCellTooltipConfigured(colDef: ColDef): boolean;
/** @internal AG_GRID_INTERNAL - Compatibility path for the deprecated `tooltipField`. */
export declare function _getLegacyTooltipFieldValue(data: any, field: string, containsDots: boolean): any;
export declare function _initColTooltip(column: AgColumn): void;
