import type { UtilBeanCollection } from '../interfaces/agCoreBeanCollection';
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare const FOCUS_MANAGED_CLASS = "ag-focus-managed";
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _registerKeyboardFocusEvents(beans: UtilBeanCollection): () => void;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _isKeyboardMode(): boolean;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _findFocusableElements(rootNode: HTMLElement, exclude?: string | null, onlyUnmanaged?: boolean): HTMLElement[];
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _focusInto(rootNode: HTMLElement, up?: boolean, onlyUnmanaged?: boolean, excludeTabGuards?: boolean): boolean;
/**
 * Focuses into `rootNode` the way native Tab would: tabbable elements win. Elements with a
 * negative tabindex are only eligible when a managed-focus wrapper inside `rootNode` owns their
 * keyboard handling — bare tabindex="-1" content would otherwise be a keyboard dead end.
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function _focusIntoTabbableFirst(rootNode: HTMLElement, up?: boolean, excludeTabGuards?: boolean): boolean;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _findNextFocusableElement(params: {
    beans: UtilBeanCollection;
    rootNode: HTMLElement;
    onlyManaged?: boolean | null;
    onlyUnmanaged?: boolean;
    backwards?: boolean;
}): HTMLElement | null;
/** @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time. */
export declare function _findTabbableParent(node: HTMLElement | null, limit?: number): HTMLElement | null;
/**
 * Like Element.scrollIntoView, but only scrolls within the grid, not the page.
 *
 * @internal AG_GRID_INTERNAL - Not for public use. Can change / be removed at any time.
 */
export declare function _scrollHorizontallyToShow(target: HTMLElement): void;
